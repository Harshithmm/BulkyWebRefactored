@model List<Product>

    @* <partial name="_Notification"/>   *@  @* here if we add then this will be only for this view hence add this in the _Layout.cshtml *@
<div class="container">
    <div class="row pt-4 pb-3">
        <div class="col-6">
            <h2 class="text-primary">
                Product List
            </h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Product" asp-action="Create" class="btn btn-primary">  @*here asp-controller is not needed bcz we are in the Product controller*@
                <i class="bi bi-plus-circle"> </i> Create New Product
            </a>
        </div>
    </div>
<table class="table table-bordered table-striped">
    <thead>
    <tr>
        <th>Title</th>
        <th>ISBN</th>
        <th>Price</th>
        <th>Author</th>
        <th>Category</th>
        <th>Action</th>
    </tr>
    </thead>
    <tbody>
    @* @foreach (var obj in Model.OrderByDescending(u=>u.DisplayOrder)) *@
    @foreach (var obj in Model)  @*orderby ascending*@
    {
        <tr>
            <td>@obj.Title</td>
            <td>@obj.ISBN</td>
            <td>@obj.Price</td>
            <td>@obj.Author</td>
            @* <td>@obj.Category</td> *@
            <td>
                    <div class="w-75 btn-group" role="group">
                        <a asp-controller="Product" asp-action="Edit" asp-route-id="@obj.Id" class="btn btn-primary mx-2">
                            @* asp-route-id ="@obj.Id"  where id in the helper can have any name but should be same as the one in the controller's action method here
                            it is Edit(int? id) *@
                            <i class="bi bi-pencil-square"></i>Edit
                        </a>
                
                    <a asp-controller="Product" asp-action="Delete" asp-route-id="@obj.Id" class="btn btn-danger mx-2 ">
                                <i class="bi bi-trash"></i>Delete
                    </a>
                    </div>
            </td>
        </tr>
    }
    </tbody>
</table>
</div>